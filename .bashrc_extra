xset r rate 200 50

# Aliases
alias remove-pyc='find . -name "*.pyc" -exec rm -rf {} \;'

# Functions

# Include Work bashrc
# Load up work specific bashrc
if [ -e ~/.bashrc_work ]; then
    . ~/.bashrc_work
fi
